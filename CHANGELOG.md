# Changelog

## 3.0.0

### 新增功能

- **展开单层文件夹**：解压时若压缩包根目录只有一个子文件夹，自动跳过该包装层，将内容直接解压到目标目录，避免产生 `标题/压缩包名/文件` 三层嵌套；可在设置面板中开关
- **论坛附件 TXT 解析**：自动识别论坛帖子附件中的 `.txt` 文件，注入「解析链接」按钮；下载后提取 `magnet` / `ed2k` 链接，支持 UTF-8 / GBK 自动编码检测，可一键推送到 115
- **论坛附件 ZIP 解析**：自动识别帖子附件中的 `.zip` 文件，注入「解析压缩包」按钮；自动解析压缩包内所有 `.txt` 和 `.xlsx` 文件中的下载链接，并汇总展示
- **加密 ZIP 支持**：将 JSZip 替换为 zip.js，支持 AES-256 / ZipCrypto 加密格式；遇到加密压缩包时弹出密码输入框，密码错误时显示提示并允许重试，密码正确后复用已下载的文件缓存无需重复下载

### 优化 / 修复

- 链接面板移除序号，避免全选复制时混入多余字符
- 移除链接面板内的原始 TXT 内容展示区，减少视觉冗余
- 关闭按钮文字改为根据入口类型动态显示（「解析链接」/ 「解析压缩包」）

### 依赖变更

- 新增 `@require` [zip.js](https://github.com/gildas-lormeau/zip.js) 2.7.52（替代 JSZip，支持加密 ZIP）
- 新增 `@require` [SheetJS xlsx](https://github.com/SheetJS/sheetjs) 0.18.5（XLSX 文件解析）

> ⚠️ 升级到 3.0.0 后需要**重新安装脚本**，Tampermonkey / Violentmonkey 才会下载并缓存新的依赖库。

## 2.3.0

- 新增推送后自动解压功能
- 新增解压后自动删除原压缩包开关
- 优化自动解压轮询参数
- 优化解压状态面板展示
- 修复标题文件夹归档逻辑

## 2.2.0

- 新增集中式设置面板
- 新增标题清洗实时预览
- 优化下载卡片 UI
- 去除调试日志

## 2.1.0

- 支持按清洗后标题创建 115 子目录
- 支持将 magnet / ed2k 推送到标题文件夹
- 支持复制清洗标题和有效链接